// Optional: Setup file for Vitest (runs before all tests)
// You can add any global mocks or browser globals here if needed.

// Example: mock window properties if needed
// globalThis.window = globalThis.window || {};
// globalThis.document = globalThis.document || {};

// Example: mock socketClient for browser code
// globalThis.window.socketClient = { on: () => {}, off: () => {}, isSocketConnected: () => true, startMonitoring: () => {}, stopMonitoring: () => {} };

// Add more setup as needed for your tests
